const postValidate = (req, res, next) => {
    // const {dato, dato, dato} = req.body;
    // if(!dato || !dato) res.status(400).json({error: "Faltan Datos"});
    // next();
};

module.exports = postValidate;